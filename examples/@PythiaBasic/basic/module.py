def ping_from_module(*args):
    """
    You can also call functions directly from modules, not just packages!
    Returns all the arguments passed to the function
    To execute this function, call:
    ["basic.module.ping_from_module", ["string", 1, 2.3, true]] call py3_fnc_callExtension
    """
    return args
